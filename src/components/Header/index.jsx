import React from 'react';

export default function Header() {
    return <nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo">Disease.sh client</a>
        </div>
    </nav>;
}
